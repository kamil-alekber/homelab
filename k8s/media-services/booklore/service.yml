---
apiVersion: v1
kind: Service
metadata:
  name: booklore
  namespace: media-services
  labels:
    app: booklore
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: booklore
