---
apiVersion: v1
kind: Service
metadata:
  name: jellyswarm
  namespace: media-services
  labels:
    app: jellyswarm
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
  selector:
    app: jellyswarm
