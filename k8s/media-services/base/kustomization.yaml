---
# Base Kustomization for Media Services
# Provides common configuration for all media service applications
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media-services

commonLabels:
  app.kubernetes.io/part-of: media-services

resources:
  - namespace.yml
  - pvc.yml

# Common configuration for all deployments
commonAnnotations:
  description: "Media automation services"
