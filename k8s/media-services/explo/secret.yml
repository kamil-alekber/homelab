---
apiVersion: v1
kind: Secret
metadata:
  name: explo-env
  namespace: media-services
  labels:
    app: explo
type: Opaque
stringData:
  .env: |
    # ListenBrainz Configuration
    LISTENBRAINZ_USER_TOKEN=your_listenbrainz_token_here
    
    # Soulseek Configuration
    SOULSEEK_USERNAME=kamil
    SOULSEEK_PASSWORD=95782641
    SLSKD_API_KEY=ddbfbc053f4e00a306ac11c4de9fb0a0
    SLSKD_URL=http://slskd
    
    # Download Configuration
    DOWNLOAD_DIR=/data
    MIGRATE_DOWNLOADS=true
    
    # Playlist Configuration (for MPD if needed)
    # PLAYLIST_DIR=/my/playlists
    # PLAYLIST_FILE_EXTENSION=.m3u
    
    # Additional settings - adjust as needed
    # MIN_BITRATE=320
    # MAX_RESULTS=50
