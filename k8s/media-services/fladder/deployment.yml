---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fladder
  namespace: media-services
  labels:
    app: fladder
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: fladder
  template:
    metadata:
      labels:
        app: fladder
    spec:
      containers:
      - name: fladder
        image: ghcr.io/donutware/fladder:latest
        ports:
        - name: http
          containerPort: 80
          protocol: TCP
        env:
        - name: BASE_URL
          value: "https://jellyfin.clusterlab.cc"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
