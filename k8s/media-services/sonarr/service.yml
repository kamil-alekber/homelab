---
apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: media-services
  labels:
    app: sonarr
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: sonarr
