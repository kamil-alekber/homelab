---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: profilarr
  namespace: media-services
  labels:
    app: profilarr
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: profilarr
  endpoints:
  - port: http
    path: /
    interval: 30s
    scrapeTimeout: 10s
