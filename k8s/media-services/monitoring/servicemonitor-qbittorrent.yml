---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: qbittorrent
  namespace: media-services
  labels:
    app: qbittorrent
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: qbittorrent
  endpoints:
  - port: http
    path: /
    interval: 30s
    scrapeTimeout: 10s
