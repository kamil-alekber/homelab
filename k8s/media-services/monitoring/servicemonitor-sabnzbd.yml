---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sabnzbd
  namespace: media-services
  labels:
    app: sabnzbd
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: sabnzbd
  endpoints:
  - port: http
    path: /
    interval: 30s
    scrapeTimeout: 10s
