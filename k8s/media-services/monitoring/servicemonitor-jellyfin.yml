---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: jellyfin
  namespace: media-services
  labels:
    app: jellyfin
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: jellyfin
  endpoints:
  - port: http
    path: /
    interval: 30s
    scrapeTimeout: 10s
