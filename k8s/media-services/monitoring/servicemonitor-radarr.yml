---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: radarr
  namespace: media-services
  labels:
    app: radarr
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: radarr
  endpoints:
  - port: http
    path: /
    interval: 30s
    scrapeTimeout: 10s
