---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: whisparr
  namespace: media-services
  labels:
    app: whisparr
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: whisparr
  endpoints:
  - port: http
    path: /
    interval: 30s
    scrapeTimeout: 10s
