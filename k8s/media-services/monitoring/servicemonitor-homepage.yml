---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: homepage
  namespace: media-services
  labels:
    app: homepage
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: homepage
  endpoints:
  - port: http
    path: /
    interval: 30s
    scrapeTimeout: 10s
