---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sonarr
  namespace: media-services
  labels:
    app: sonarr
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: sonarr
  endpoints:
  - port: http
    path: /
    interval: 30s
    scrapeTimeout: 10s
