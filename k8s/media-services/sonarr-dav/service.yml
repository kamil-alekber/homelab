---
apiVersion: v1
kind: Service
metadata:
  name: sonarr-dav
  namespace: media-services
  labels:
    app: sonarr-dav
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: sonarr-dav
