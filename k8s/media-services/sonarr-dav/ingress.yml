apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-dav
  namespace: media-services
  annotations:
    gethomepage.dev/href: "http://sonarr-dav.clusterlab.cc"
    gethomepage.dev/enabled: "true"
    gethomepage.dev/description: "TV series collection manager for Usenet and BitTorrent users (DAV)."
    gethomepage.dev/group: Media
    gethomepage.dev/icon: sonarr.png
    gethomepage.dev/name: Sonarr DAV
    gethomepage.dev/pod-selector: "app=sonarr-dav"

spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sonarr-dav.clusterlab.cc`)
      kind: Rule
      services:
        - name: sonarr-dav
          port: http
  tls:
    secretName: sonarr-dav-clusterlab-cc-tls
