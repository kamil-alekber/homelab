apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: flaresolverr
  namespace: media-services
  annotations:
    gethomepage.dev/href: "https://flaresolverr.clusterlab.cc"
    gethomepage.dev/enabled: "true"
    gethomepage.dev/description: "Cloudflare bypass proxy"
    gethomepage.dev/group: Utilities
    gethomepage.dev/icon: flaresolverr.png
    gethomepage.dev/name: FlareSolverr
    gethomepage.dev/pod-selector: "app=flaresolverr"

spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`flaresolverr.clusterlab.cc`)
      kind: Rule
      services:
        - name: flaresolverr
          port: http
  tls:
    secretName: flaresolverr-clusterlab-cc-tls
