apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: radarr-dav-clusterlab-cc
  namespace: media-services
spec:
  secretName: radarr-dav-clusterlab-cc-tls
  dnsNames:
    - radarr-dav.clusterlab.cc
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
