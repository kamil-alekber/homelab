---
apiVersion: v1
kind: Service
metadata:
  name: plex-dav
  namespace: media-services
  labels:
    app: plex-dav
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: plex-dav
