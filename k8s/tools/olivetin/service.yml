---
apiVersion: v1
kind: Service
metadata:
  name: olivetin-service
  namespace: default
  labels:
    app: olivetin
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: olivetin
