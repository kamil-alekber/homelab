---
apiVersion: v1
kind: Service
metadata:
  name: automad-service
  namespace: default
  labels:
    app: automad
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: automad
