---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: automad-data-samba
  namespace: default
  labels:
    app: automad
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: samba-storage
  resources:
    requests:
      storage: 5Gi
