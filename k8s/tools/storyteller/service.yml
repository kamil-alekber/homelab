---
apiVersion: v1
kind: Service
metadata:
  name: storyteller-service
  namespace: default
  labels:
    app: storyteller
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: storyteller
