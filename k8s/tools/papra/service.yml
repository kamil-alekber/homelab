---
apiVersion: v1
kind: Service
metadata:
  name: papra-service
  namespace: default
  labels:
    app: papra
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: papra
