---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: papra-data-samba
  namespace: default
  labels:
    app: papra
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: samba-storage
  resources:
    requests:
      storage: 5Gi
