---
apiVersion: v1
kind: Service
metadata:
  name: registry-service
  namespace: default
  labels:
    app: registry
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5000
      targetPort: http
      protocol: TCP
  selector:
    app: registry
