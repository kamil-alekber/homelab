---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gogs-data
  namespace: default
  labels:
    app: gogs
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

