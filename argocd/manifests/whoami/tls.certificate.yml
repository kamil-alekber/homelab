apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: whoami-clusterlab-cc
  namespace: whoami
spec:
  secretName: whoami-clusterlab-cc-tls
  dnsNames:
    - whoami.clusterlab.cc
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
